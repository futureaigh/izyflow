[build]
builder = "NIXPACKS"

[deploy]
startCommand = "node --loader tsx --env-file=.env server.ts"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[[services]]
http_port = 3000